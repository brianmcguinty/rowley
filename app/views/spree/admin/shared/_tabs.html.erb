<%= tab(:overview, :route => :admin_dashboard, :icon => 'icon-dashboard') if can?(:manage, :all) %>
<%= tab(:orders, :payments, :creditcard_payments, :shipments, :credit_cards, :return_authorizations, :icon => 'icon-shopping-cart') if can?(:index, Spree::Order) %>
<%= tab(:products, :option_types, :properties, :prototypes, :variants, :product_properties, :taxons, :icon => 'icon-th-large') if can?(:manage, :all) %>
<%= tab(:reports, :icon => 'icon-file') if can?(:manage, :all) %>
<%= tab(:configurations, :general_settings, :mail_methods, :tax_categories, :zones, :states, :payment_methods, :inventory_settings, :taxonomies, :shipping_methods, :trackers, :label => 'configuration', :icon => 'icon-wrench', :url => spree.edit_admin_general_settings_path) if can?(:manage, :all) %>
